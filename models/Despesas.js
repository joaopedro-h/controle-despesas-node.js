class Despesa {
    constructor(id, descricao, valor, categoria) {
        
    this.id = id,
    this.descricao = descricao,
    this.valor = valor,
    this.categoria = categoria

    }
}

module.exports = Despesa;